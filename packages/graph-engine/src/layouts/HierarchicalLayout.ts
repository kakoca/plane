import { GraphNode, GraphEdge } from '../types';

class HierarchicalLayout {
  apply(nodes: GraphNode[], edges: GraphEdge[]): GraphNode[] {
    // Stub implementation - will be implemented later
    return nodes;
  }
}

export default HierarchicalLayout;